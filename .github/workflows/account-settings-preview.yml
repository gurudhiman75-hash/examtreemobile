name: Account Settings preview

on:
  pull_request:
    paths:
      - 'lib/core/theme/**'
      - 'lib/features/profile/**'
      - 'test/features/profile/**'
      - '.github/workflows/account-settings-preview.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  preview:
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Install Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Stage pinned fonts for preview rendering
        run: |
          mkdir -p test/features/profile/presentation/account_previews
          cp "$FLUTTER_ROOT/bin/cache/artifacts/material_fonts/Roboto-Regular.ttf" \
            test/features/profile/presentation/account_previews/Roboto-Regular.ttf
          cp "$FLUTTER_ROOT/bin/cache/artifacts/material_fonts/MaterialIcons-Regular.otf" \
            test/features/profile/presentation/account_previews/MaterialIcons-Regular.otf

      - name: Render Account Settings phone previews
        run: |
          flutter test --update-goldens \
            test/features/profile/presentation/account_settings_preview.dart

      - name: Publish Account Settings screenshots
        uses: actions/upload-artifact@v4
        with:
          name: examtree-account-settings-screenshots
          path: test/features/profile/presentation/account_previews/*.png
          if-no-files-found: error
          retention-days: 14
