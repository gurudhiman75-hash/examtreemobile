name: Test attempt preview

on:
  pull_request:
    paths:
      - 'lib/core/theme/**'
      - 'lib/features/test_attempt/**'
      - 'test/features/test_attempt/**'
      - '.github/workflows/test-attempt-preview.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  preview:
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Install Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Stage pinned fonts for preview rendering
        run: |
          mkdir -p test/features/test_attempt/presentation/attempt_previews
          cp "$FLUTTER_ROOT/bin/cache/artifacts/material_fonts/Roboto-Regular.ttf" \
            test/features/test_attempt/presentation/attempt_previews/Roboto-Regular.ttf
          cp "$FLUTTER_ROOT/bin/cache/artifacts/material_fonts/MaterialIcons-Regular.otf" \
            test/features/test_attempt/presentation/attempt_previews/MaterialIcons-Regular.otf

      - name: Render test attempt phone previews
        run: |
          flutter test --update-goldens \
            test/features/test_attempt/presentation/test_attempt_preview.dart

      - name: Publish test attempt screenshots
        uses: actions/upload-artifact@v4
        with:
          name: examtree-test-attempt-screenshots
          path: test/features/test_attempt/presentation/attempt_previews/*.png
          if-no-files-found: error
          retention-days: 14
