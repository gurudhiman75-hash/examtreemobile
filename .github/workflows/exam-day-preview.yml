name: Exam-Day preview

on:
  pull_request:
    paths:
      - 'lib/core/theme/**'
      - 'lib/features/exam_day/**'
      - 'test/features/exam_day/**'
      - '.github/workflows/exam-day-preview.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  preview:
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Install Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Stage pinned fonts for preview rendering
        run: |
          mkdir -p test/features/exam_day/presentation/previews
          cp "$FLUTTER_ROOT/bin/cache/artifacts/material_fonts/Roboto-Regular.ttf" \
            test/features/exam_day/presentation/previews/Roboto-Regular.ttf
          cp "$FLUTTER_ROOT/bin/cache/artifacts/material_fonts/MaterialIcons-Regular.otf" \
            test/features/exam_day/presentation/previews/MaterialIcons-Regular.otf

      - name: Render Exam-Day phone previews
        run: |
          flutter test --update-goldens \
            test/features/exam_day/presentation/exam_day_preview.dart

      - name: Publish Exam-Day screenshots
        uses: actions/upload-artifact@v4
        with:
          name: examtree-exam-day-screenshots
          path: test/features/exam_day/presentation/previews/*.png
          if-no-files-found: error
          retention-days: 14
